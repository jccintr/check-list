import { Text } from 'react-native';
import React from 'react';
import { cores } from '../globalStyle';

const EmptyList = () => {
    return <Text style={{color: cores.azul}}>Tem nada ainda parceiro !</Text>
}

export default EmptyList
